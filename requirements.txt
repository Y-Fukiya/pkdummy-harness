# Core runtime for the main harness (generate -> validate -> NCA -> profiles).
# Optional tool groups install via extras, e.g. `pip install .[harvest]` (web
# harvesting) or `pip install .[jobs]` (job/excluded CSV utilities).
PyYAML>=6.0
